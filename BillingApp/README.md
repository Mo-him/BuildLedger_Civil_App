# Civil Billing API

FastAPI backend for the Civil Billing App.

## Run

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Open:
- http://127.0.0.1:8000
- http://127.0.0.1:8000/docs

Database: SQL Server database `CivilBillingApp`.
